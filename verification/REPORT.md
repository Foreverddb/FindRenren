# 寻之绘馆验证报告

验证日期：2026-07-27  
测试地址：http://127.0.0.1:4180/findxx/（GitHub Pages 项目子路径模拟）

## 验证范围

- 桌面视口：1440 × 1000
- 手机视口：390 × 844
- 测试颜色：湖蓝 `#277F9F`、松绿 `#2F8A66`、鸢尾紫 `#7651A8`
- 自定义文字：底部 `找星河。`、围脖同步为 `找星河`，文字颜色 `#FFD84A`
- 人物署名：`Renren` 单独修改为 `Luna`，验证标题栏内垂直居中
- 人物测试图：`portrait-test-source.png`，分别验证完整图片和智能抠图
- 人物相框：程序化绘制 `264 × 294 px` 圆角外框，图片安全显示区 `218 × 246 px`
- 人物边缘：移除原图像素抽取叠层，改用纯色底、连续三层曲线描边和圆角署名牌
- 人物背景：统一纯色 `#F4E8CB`，不再采样或模糊原图纹理
- 替换范围：移除分区开关，荧光棒、围脖、身体和文字背景始终完整替换
- 模型冷启动：同时暂缓两份本地权重响应，验证编辑器先就绪、双模型静默预载与点击后自动续接
- 抠图质量：默认“高”，支持 `MODNet q8` 低质量快速抠图与 `BEN2 FP16` 高质量精细抠图
- 围脖 Logo 图片区：扩展至“找xx”右侧整段，包含原 `Renren` 与 `ShuZhao` 区域
- 默认围脖 Logo：默认选中文字模式，内容为 `StarHoney`
- 自定义围脖 Logo：上传 `favicon.svg`，验证整段清除、等比适配、单独恢复和全局恢复
- 围脖文字 Logo：输入 `Moonlight`，验证飘逸手写体、逐字旋转和沿围脖曲线排版
- 导出格式：PNG，1176 × 1224 px

## 抠图方案

| 方案 | 许可证 | 结论 |
| --- | --- | --- |
| `@imgly/background-removal` | AGPL-3.0 | 浏览器端能力完整，但许可证约束较强，未采用 |
| `Transformers.js 4.2` + `Xenova/MODNet` | Apache-2.0 | 256 × 256、q8 量化、6,632,188 bytes，作为“低”质量快速方案 |
| `Transformers.js 4.2` + `onnx-community/BEN2-ONNX` | MIT | 1024 × 1024、FP16、219,121,675 bytes，作为默认“高”质量方案 |

- 部署方式：两套权重与配置均位于当前站点基址下的 `models/`，不请求外部模型服务
- 加载方式：编辑器完成首屏渲染后，两套模型在浏览器空闲时同时静默预载，不阻塞调色和文字编辑
- 等待反馈：仅当用户点击当前质量且对应模型尚未就绪时显示该模型进度，下载完成后自动开始抠图
- 结果缓存：每张上传图片分别缓存高、低质量透明结果，切换质量或完整/抠图模式不重复推理
- 推理方式：单线程 WASM，本地运行，失败时自动回退到完整图片模式
- 第三方来源和许可证记录见 `THIRD_PARTY_NOTICES.md`

## 功能结果

| 项目 | 结果 |
| --- | --- |
| 手动调色板与 HEX 值同步 | 通过 |
| 荧光棒替色并保留柔光 | 通过 |
| 围脖替色并保留织物明暗 | 通过 |
| 身体替色并保留纹理 | 通过 |
| 文字底板替色，白字不受影响 | 通过 |
| 替换区域选项已移除 | 通过，页面控件数量为 0 |
| 四个目标区域始终完整替换 | 通过 |
| 替换强度 0% 恢复原图 | 通过 |
| 人物、黄色嘴部、黑色轮廓保护 | 通过 |
| 原图/效果切换 | 通过 |
| “找”字由前端统一渲染 | 通过 |
| “找”字仅在编辑栏固定不可修改 | 通过 |
| 底部“找”和后续文字字体一致 | 通过 |
| 后半段文字内容修改 | 通过 |
| 后半段文字颜色修改 | 通过 |
| 围脖标题与底部文字同步 | 通过 |
| 围脖保持原有无句号格式 | 通过 |
| 围脖文字不侵入英文署名区域 | 通过 |
| 围脖文字不侵入上下边线 | 通过 |
| 默认 Logo 选中文字 `StarHoney` | 通过 |
| Logo 图片区覆盖原 `Renren` 与 `ShuZhao` 整段 | 通过，左右子区域均发生变化 |
| 围脖 Logo 图片上传与等比适配 | 通过 |
| 原 Logo 局部清除且保留星形装饰 | 通过 |
| 围脖 Logo 单独恢复默认文字 `StarHoney` | 通过 |
| 围脖 Logo 图片/文字模式切换 | 通过 |
| 围脖文字 Logo 内容修改 | 通过，测试 `Moonlight` |
| 围脖文字 Logo 使用飘逸手写体 | 通过 |
| 围脖文字 Logo 沿围脖方向弯曲 | 通过，右端较左端上扬 21.33 px |
| 围脖文字 Logo 避让同步文字、星形与边线 | 通过，字形范围 x=627–832、y=543–617 |
| 文字模式返回图片模式恢复原图 | 通过 |
| 全局恢复默认文字 `StarHoney` Logo | 通过 |
| 人物署名单独修改 | 通过 |
| 人物署名手写体与原图一致 | 通过 |
| 人物署名向下调整并完整位于标题栏 | 通过，字形 y=921–951 |
| 人物署名上下中心落点 | 通过，中心 y=936 |
| 人物完整图片填充并自动裁剪 | 通过 |
| 高质量 BEN2 抠图与透明合成 | 通过，透明区 43.30%，软边 2.71% |
| 低质量 MODNet 抠图与透明合成 | 通过，透明区 40.77%，软边 4.75% |
| 高/低质量切换 | 通过，两套结果独立缓存且即时切换 |
| 完整/抠图模式即时切换 | 通过，复用当前质量缓存 |
| 默认抠图质量 | 通过，首次进入与恢复默认后均为“高” |
| 人物外框使用平滑圆角重新绘制 | 通过 |
| 原图像素抽取式杂乱边框已移除 | 通过 |
| 外框上、左、右边线连续 | 通过，覆盖率均为 100%，最大断点 0 px |
| 外框圆角平滑展开 | 通过，曲线跨度 11 px 且单调连续 |
| 底部署名牌与两侧丝带平滑衔接 | 通过 |
| 完整图片严格限制在安全显示区 | 通过，框外四边哈希完全不变 |
| 高、低质量抠图人物严格限制在安全显示区 | 通过，两者框外四边哈希完全不变 |
| 完整图片保留纯色相纸边 | 通过，纯色像素 4,276 |
| 抠图模式使用纯色内框背景 | 通过，高质量占比 51.71%，低质量占比 49.49% |
| 原人物残影与边框杂色清除 | 通过 |
| 人物图片单独恢复原图 | 通过 |
| 全局恢复默认人物 | 通过 |
| 模型使用本站静态资源 | 通过，外部模型请求 0 |
| GitHub Pages 项目子路径加载 | 通过，入口、原图、WASM、配置和两套模型均从 `/findxx/` 加载 |
| 进入页面后自动静默预载两套模型 | 通过，两份权重请求同时发起且早于智能抠图点击 |
| 静默预载不阻塞编辑器 | 通过，首屏遮罩已关闭且调色控件可用 |
| 静默预载不显示进度 | 通过 |
| 未加载完成时点击显示对应质量进度 | 通过，高、低质量均为 0% → 100% |
| 两套模型就绪后自动继续抠图 | 通过，均无需再次点击 |
| 两套模型就绪后收起进度条 | 通过，`low=ready`、`high=ready` |
| 默认“恋恋。”与白色恢复 | 通过 |
| 默认 `Renren` 署名恢复 | 通过 |
| 长文本自动适配底板宽度 | 通过 |
| PNG 下载与原始尺寸 | 通过 |
| 移动端无横向溢出 | 通过 |
| 浏览器控制台错误 | 0 |

## 像素断言

自动化测试对原图与湖蓝结果的 RGB 通道绝对差值求和。目标区域应显著变化，保护区域应保持为 0。

| 采样区域 | 通道差值 | 判定 |
| --- | ---: | --- |
| 荧光棒 | 194 | 已替换 |
| 围脖 | 232 | 已替换 |
| 身体 | 190 | 已替换 |
| 文字底板 | 195 | 已替换 |
| 人物照片 | 0 | 已保护 |
| 黄色嘴部 | 0 | 已保护 |
| 黑色轮廓 | 0 | 已保护 |

## 验证产物

- 机器结果：`results.json`
- 默认文字 `StarHoney`：`desktop-blue.png`
- 围脖文字 Logo：`desktop-custom-text-logo.png`
- 围脖图片 Logo：`desktop-custom-logo.png`
- 高/低模型等待截图：`desktop-model-loading-high.png`、`desktop-model-loading-low.png`
- 完整替换细节：`portrait-frame-full-detail.png`
- 高/低质量抠图细节：`portrait-frame-cutout-high.png`、`portrait-frame-cutout-low.png`
- 完整页面截图：`desktop-portrait-full.png`、`desktop-portrait-cutout-high.png`、`desktop-portrait-cutout-low.png`
- 实际首屏截图：`desktop-live-viewport.png`
- 移动端截图：`mobile-green.png`
- 实际导出：`export-custom-design.png`
